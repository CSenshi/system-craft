generator client {
  provider        = "prisma-client"
  output          = "../src/prisma/generated"
  engineType      = "client"
  moduleFormat    = "cjs"
}

datasource db {
  provider = "postgresql"
}

model ShortendUrls {
  id  BigInt @id
  url String

  @@map("shortend_urls")
}
