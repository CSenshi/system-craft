# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build @system-design-implementations/url-shortener`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t system-design-implementations-url-shortener`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system system-design-implementations-url-shortener && \
          adduser --system -G system-design-implementations-url-shortener system-design-implementations-url-shortener

COPY apps/url-shortener/dist system-design-implementations-url-shortener/
COPY apps/url-shortener/package.json system-design-implementations-url-shortener/
RUN chown -R system-design-implementations-url-shortener:system-design-implementations-url-shortener .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix system-design-implementations-url-shortener --omit=dev -f install

CMD [ "node", "system-design-implementations-url-shortener" ]
