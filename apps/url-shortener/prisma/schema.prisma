generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model ShortendUrls {
  id  Int    @id
  url String

  @@map("shortend_urls")
}
